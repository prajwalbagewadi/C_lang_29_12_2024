/**
 * sizeof
 * sizeof (operand)
 * , 
 * (operand1 , operand2)
 * ?:
 * (condition ? operand1 : operand2)
 * . dot -> used to access struct members
 * cast
 * (new_type) operand; 
 * & returns the address of a var
 * * pointer var
 */